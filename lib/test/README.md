The test files are to display the UI when no usb device is available.
when testing:
  1. rename test_main.dart to main.dart and use it to replace main.dart
  2. rename test_receive_data_func.dart to receive_data_func.dart and use it to replace the actual file

this will simulate the UI without having to plug in an actual USB device.
